node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.env
.env.*.local
.DS_Store
.meeseeks/
settings.json
.claude/settings.local.json

# Live workspace data. The default workspace root is now XDG (~/.local/share/meeseeks),
# not this checkout, but `meeseeks .` still points a server at the repo directly, so
# these stay ignored as a backstop. The first two are the board-era names, kept for
# anyone with an unmigrated checkout.
boards/
project.yaml
workspace.yaml
workflows/
projects/
prompts/
*.pre-migrate
