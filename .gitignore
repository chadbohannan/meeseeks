node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.env
.env.*.local
.DS_Store
.meeseeks/
settings.json

# Live workspace data. This repository is also the workspace it supervises, so
# a running server writes its state right here; none of it is source. The first
# two are the board-era names, kept for anyone with an unmigrated checkout.
boards/
project.yaml
workspace.yaml
workflows/
projects/
prompts/
*.pre-migrate
